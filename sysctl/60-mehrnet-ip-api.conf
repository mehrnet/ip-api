# Dedicated low-memory responder host. The Nginx listeners use a matching
# backlog; zram is configured separately as a safety net, not a performance
# cache.
net.core.somaxconn = 4096
net.ipv4.tcp_max_syn_backlog = 4096
net.ipv4.tcp_syncookies = 1
vm.swappiness = 10
