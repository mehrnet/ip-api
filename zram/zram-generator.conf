[zram0]
# A 1 GiB host gets a 512 MiB logical zram swap device. lzo-rle is selected
# for low CPU overhead; the service has a very small steady-state footprint.
zram-size = ram / 2
compression-algorithm = lzo-rle
swap-priority = 100
